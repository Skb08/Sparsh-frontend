import { BrowserRouter, Routes, Route } from 'react-router-dom'
import Router from './components/Navbar/Router';

function App() {
  return (
      <BrowserRouter>
        <Router/>
      </BrowserRouter>   
  );
}

export default App;